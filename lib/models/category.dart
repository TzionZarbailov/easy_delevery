class Category {
  final String id; //*This is the id of the category
  final String name; //*This is the name of the category

  const Category({
    required this.id,
    required this.name,
  });
}
